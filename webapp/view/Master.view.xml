<mvc:View controllerName="com.nexus.asset.controller.Master" xmlns="sap.m"
	xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
	xmlns:mvc="sap.ui.core.mvc"   xmlns:u="sap.ui.unified"
    xmlns:semantic="sap.f.semantic" xmlns:tree="sap.ui.table">
    <f:DynamicPage id="dynamicPageId" toggleHeaderOnTitleClick="false" title="{i18n>assetTitle}">
        <f:title>
            <f:DynamicPageTitle id="dynamicPageTitle">
                <f:heading>
                    <Title id="pageTitle" text="Asset Hierarchy" />
                </f:heading>
                <f:actions>
                    <!--actions-->
                </f:actions>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
            <VBox id="mainVBox">
                
                <tree:TreeTable
                    id="TreeTableBasic"
                    rows="{path:'LocalDataModel>/treeTable'}"
                    selectionMode="Single"
                    rowSelectionChange="onRowSelectionChange"
                    enableSelectAll="false"
                    ariaLabelledBy="pageTitle">
                    <tree:extension>
                    <OverflowToolbar style="Clear">
                        <ComboBox items="{LocalDataModel>/treeList}" selectedKey="{LocalDataModel>/selectedNode}" selectionChange="onNodeSelect">
                    <core:Item key="{LocalDataModel>CV_ID}" text="{LocalDataModel>Name}" />
                </ComboBox>
                    </OverflowToolbar>
                </tree:extension>
                <tree:columns>
                    <tree:Column id="colCategories" width="13rem">
                        <Label id="lblCategories" text="Asset Name"/>
                        <tree:template>
                            <Text id="txtCategoryName" text="{= ${LocalDataModel>haschild} ? '> ' + ${LocalDataModel>Name} : ${LocalDataModel>Name} }" />
                        </tree:template>
                    </tree:Column>
                </tree:columns>
            </tree:TreeTable>
            </VBox>
        </f:content>
    </f:DynamicPage>
</mvc:View>
